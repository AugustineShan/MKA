# 首页 · 文件夹总览 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 workbench 加全局「首页」，其第一个 subtab「文件夹总览」用一张表展示所有公司文件夹健康度，并提供「一键归档历史模型」动作（yaml1 → `Agent/yaml1history/`、根目录 Excel → `Agent/Modelhistory/`、`~$` 锁文件直接删）。

**Architecture:** 后端在 `src/workbench.py` 加两个端点（`GET /api/home/folder-overview`、`POST /api/companies/{id}/archive-models`）+ 三个纯函数 helper（`_pipeline_stage`、`_folder_overview_signals`、`_archive_models`），复用 `src/company_paths.py` 既有路径 helper。前端在 `app/src/App.tsx` 加 `homeActive` 模式，Sidebar 顶部加「首页」条目（视觉同公司项），新增 `FolderOverview` 组件渲染信号表 + 归档按钮。

**Tech Stack:** Python 3.11 + FastAPI（后端）、React + TypeScript + Vite（前端）、pytest（测试）。

**Spec:** `docs/superpowers/specs/2026-06-26-首页文件夹总览-design.md`

---

## File Structure

- **Modify** `src/workbench.py` — 加 3 个 helper + 2 个端点
- **Create** `tests/test_workbench_home.py` — 后端 TDD
- **Modify** `app/src/types.ts` — 加 `HomeFolderOverview` 等类型
- **Modify** `app/src/App.tsx` — `homeActive` 状态、Sidebar 首页条目、主区条件渲染、`FolderOverview` 组件
- **Modify** `app/src/styles.css` — 首页条目 + 信号表样式
- **Modify** `docs/ARCHITECTURE.md`、`docs/CHANGELOG.md`、`docs/数据流水线.md` — 同步文档

---

## Task 1: 后端 — `_pipeline_stage` helper（TDD）

**Files:**
- Modify: `src/workbench.py`（在 `_core_assumption` 函数后，约 line 311 之后插入）
- Test: `tests/test_workbench_home.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_workbench_home.py`：

```python
from __future__ import annotations

from pathlib import Path

from src.workbench import _pipeline_stage


def _make_company(tmp_path: Path, name: str = "测试公司_002946") -> Path:
    company = tmp_path / "companies" / name
    company.mkdir(parents=True)
    return company


def test_stage_uninitialized_no_db(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    assert _pipeline_stage(company) == "未初始化"


def test_stage_init_done(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    (company / "Agent").mkdir()
    (company / "Agent" / "data.db").write_bytes(b"")
    (company / "Agent" / "defaults.yaml").write_text("meta: {}")
    assert _pipeline_stage(company) == "初始化完毕"


def test_stage_core_assumption_done(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    (company / "Agent").mkdir()
    (company / "Agent" / "data.db").write_bytes(b"")
    (company / "Agent" / "defaults.yaml").write_text("meta: {}")
    (company / "测试公司-20260626-核心假设.md").write_text("# 假设")
    assert _pipeline_stage(company) == "核心假设完毕"


def test_stage_modeled(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    (company / "Agent").mkdir()
    (company / "Agent" / "data.db").write_bytes(b"")
    (company / "Agent" / "defaults.yaml").write_text("meta: {}")
    (company / "Agent" / "yaml1_测试公司_20260626.yaml").write_text("meta: {}")
    assert _pipeline_stage(company) == "建模完毕"


def test_stage_modeled_with_da(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    (company / "Agent").mkdir()
    (company / "Agent" / "data.db").write_bytes(b"")
    (company / "Agent" / "defaults.yaml").write_text("meta: {}")
    (company / "Agent" / "yaml1_测试公司_20260626.yaml").write_text("meta: {}")
    (company / "Agent" / "da_schedule.yaml").write_text("meta: {}")
    assert _pipeline_stage(company) == "建模完毕且有DA表"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `py -m pytest tests/test_workbench_home.py -v`
Expected: FAIL `ImportError: cannot import name '_pipeline_stage'`

- [ ] **Step 3: 写最小实现**

在 `src/workbench.py` `_core_assumption` 函数后插入：

```python
def _pipeline_stage(company_dir: Path) -> str:
    """建模管线推进状态：未初始化 / 初始化完毕 / 核心假设完毕 / 建模完毕 / 建模完毕且有DA表。"""
    agent = agent_dir(company_dir)
    if not (agent / "data.db").exists() or not company_defaults_path(company_dir).exists():
        return "未初始化"
    if _latest_yaml1(company_dir) is not None:
        if da_schedule_path(company_dir).exists():
            return "建模完毕且有DA表"
        return "建模完毕"
    if _core_assumption(company_dir) is not None:
        return "核心假设完毕"
    return "初始化完毕"
```

注：`agent_dir`、`company_defaults_path`、`da_schedule_path` 已在 workbench.py import（line 46-63）。

- [ ] **Step 4: 跑测试确认通过**

Run: `py -m pytest tests/test_workbench_home.py -v`
Expected: 5 passed

- [ ] **Step 5: 提交**

```bash
git add tests/test_workbench_home.py src/workbench.py
git commit -m "feat(workbench): add _pipeline_stage helper for folder overview"
```

---

## Task 2: 后端 — `_folder_overview_signals` helper + `GET /api/home/folder-overview` 端点（TDD）

**Files:**
- Modify: `src/workbench.py`（helper 插在 `_pipeline_stage` 后；端点插在 `list_companies` 端点后，约 line 2392 后）
- Test: `tests/test_workbench_home.py`

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_workbench_home.py`：

```python
from src.workbench import _folder_overview_signals, _count_files


def test_count_files_handles_missing_dir(tmp_path: Path) -> None:
    assert _count_files(tmp_path / "nope") == 0


def test_count_files_counts_recursively(tmp_path: Path) -> None:
    d = tmp_path / "d"
    d.mkdir()
    (d / "a.pdf").write_bytes(b"")
    (d / "sub").mkdir()
    (d / "sub" / "b.pdf").write_bytes(b"")
    assert _count_files(d) == 2


def test_signals_full(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    agent = company / "Agent"
    agent.mkdir()
    (agent / "data.db").write_bytes(b"")
    (agent / "defaults.yaml").write_text("meta: {}")
    (agent / "yaml1_测试公司_20260624.yaml").write_text("meta: {}")
    (agent / "yaml1_测试公司_20260626.yaml").write_text("meta: {}")
    (agent / "da_schedule.yaml").write_text("meta: {}")
    (company / "测试公司Model-260624.xlsx").write_bytes(b"")
    (company / "测试公司Model-260626.xlsx").write_bytes(b"")
    (company / "~$测试公司Model-260624.xlsx").write_bytes(b"")
    for sub in ("研报", "纪要", "收集", "重要文件"):
        d = company / sub
        d.mkdir()
        (d / "x.pdf").write_bytes(b"")
    sv = company / "Skills素材包"
    for sub in (
        "LOAD外部EXCEL模型理解器（一次最多一个）",
        "BRKD业务理解器（研报和纪要放在这里）",
        "最高权重材料-放Agent最应对齐的材料",
        "ADJ增量信息（用来改模型的边际信息）",
        "PJBG评级报告素材区",
    ):
        d = sv / sub
        d.mkdir(parents=True)
        (d / "y.pdf").write_bytes(b"")

    signals = _folder_overview_signals(company)

    assert signals["pipeline_stage"] == "建模完毕且有DA表"
    assert signals["yaml1_date"] == "2026-06-26"
    assert signals["yaml1_versions"] == 2
    assert signals["yaml1_archive_eligible"] is True
    assert signals["root_models"] == {"excel_count": 2, "lock_count": 1, "archive_eligible": True}
    assert signals["workbench_materials"] == {"reports": 1, "notes": 1, "collected": 1, "important": 1}
    assert signals["agent_materials"]["load"] == 1
    assert signals["agent_materials"]["pjbg"] == 1


def test_signals_no_yaml1_date_null(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    (company / "Agent").mkdir()
    (company / "Agent" / "data.db").write_bytes(b"")
    (company / "Agent" / "defaults.yaml").write_text("meta: {}")
    signals = _folder_overview_signals(company)
    assert signals["yaml1_date"] is None
    assert signals["yaml1_versions"] == 0
    assert signals["root_models"]["archive_eligible"] is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `py -m pytest tests/test_workbench_home.py -v`
Expected: FAIL `ImportError: cannot import name '_folder_overview_signals'`

- [ ] **Step 3: 写最小实现**

在 `src/workbench.py` `_pipeline_stage` 后插入：

```python
import shutil  # 若文件顶部已 import 则不重复；放函数内 import 也行

_YAML1_DATE_RE = re.compile(r"(\d{8})")
_MODEL_DATE_RE = re.compile(r"(\d{6})")

_WORKBENCH_MATERIAL_KEYS = {
    "reports": "research_reports_dir",
    "notes": "meeting_notes_dir",
    "collected": "collection_dir",
    "important": "important_files_dir",
}

_AGENT_MATERIAL_SUBDIRS = {
    "load": "ka_model_dir",
    "brkd": "brkd_material_dir",
    "top_weight": "top_weight_material_dir",
    "adj": "adj_increment_dir",
    "pjbg": "pjbg_rating_report_dir",
}


def _count_files(path: Path) -> int:
    if not path.exists():
        return 0
    return sum(1 for _ in path.rglob("*") if _.is_file())


def _yaml1_date(path: Path) -> str | None:
    matches = _YAML1_DATE_RE.findall(path.stem)
    if not matches:
        return None
    s = matches[-1]
    return f"{s[:4]}-{s[4:6]}-{s[6:8]}"


def _folder_overview_signals(company_dir: Path) -> dict[str, Any]:
    yaml1s = sorted(agent_dir(company_dir).glob("yaml1*.yaml"))
    latest = _latest_yaml1(company_dir)
    yaml1_date = _yaml1_date(latest) if latest else None

    excels = [p for p in company_dir.glob("*.xlsx") if not p.name.startswith("~$")]
    locks = list(company_dir.glob("~$*.xlsx"))
    archive_eligible_models = len(excels) > 1 or len(locks) > 0

    workbench_materials = {
        key: _count_files(getattr(__import__("src.company_paths", fromlist=[fn]), fn)(company_dir))
        for key, fn in _WORKBENCH_MATERIAL_KEYS.items()
    }
    agent_materials = {
        key: _count_files(getattr(__import__("src.company_paths", fromlist=[fn]), fn)(company_dir))
        for key, fn in _AGENT_MATERIAL_SUBDIRS.items()
    }

    return {
        "pipeline_stage": _pipeline_stage(company_dir),
        "yaml1_date": yaml1_date,
        "yaml1_versions": len(yaml1s),
        "yaml1_archive_eligible": len(yaml1s) > 1,
        "root_models": {
            "excel_count": len(excels),
            "lock_count": len(locks),
            "archive_eligible": archive_eligible_models,
        },
        "workbench_materials": workbench_materials,
        "agent_materials": agent_materials,
    }
```

> 注：`getattr(__import__(...), fn)` 是为了避免在模块顶部再 import 一串函数；若嫌丑可改为顶部 `from src.company_paths import research_reports_dir, meeting_notes_dir, collection_dir, important_files_dir, ka_model_dir, brkd_material_dir, top_weight_material_dir, adj_increment_dir, pjbg_rating_report_dir`，然后 `_count_files(research_reports_dir(company_dir))`。**推荐用顶部 import 写法**，更清晰。实现时按推荐写法，更新测试不变。

在 `list_companies` 端点后（line 2392 后）加端点：

```python
@app.get("/api/home/folder-overview")
def home_folder_overview() -> list[dict[str, Any]]:
    out: list[dict[str, Any]] = []
    for company_dir in _company_dirs():
        meta = _read_meta(company_dir)
        name = meta.get("name") or company_dir.name.rsplit("_", 1)[0]
        code = company_dir.name.rsplit("_", 1)[-1]
        try:
            signals = _folder_overview_signals(company_dir)
        except Exception as exc:  # 单家公司异常不拖垮整表
            signals = None
            error = str(exc)
        else:
            error = None
        out.append({
            "company_id": company_dir.name,
            "name": str(name),
            "code": code,
            "signals": signals,
            "error": error,
        })
    return out
```

- [ ] **Step 4: 跑测试确认通过**

Run: `py -m pytest tests/test_workbench_home.py -v`
Expected: 8 passed

- [ ] **Step 5: 提交**

```bash
git add tests/test_workbench_home.py src/workbench.py
git commit -m "feat(workbench): add folder-overview signals + endpoint"
```

---

## Task 3: 后端 — `_archive_models` helper（TDD）

**Files:**
- Modify: `src/workbench.py`（插在 `_folder_overview_signals` 后）
- Test: `tests/test_workbench_home.py`

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_workbench_home.py`：

```python
from src.workbench import _archive_models


def test_archive_yaml1_keeps_latest_moves_rest(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    agent = company / "Agent"
    agent.mkdir()
    old = agent / "yaml1_测试公司_20260624.yaml"
    new = agent / "yaml1_测试公司_20260626.yaml"
    old.write_text("old")
    new.write_text("new")
    # 让 new 的 mtime 更新（latest_yaml1_path 按 mtime 取最新）
    import os, time
    os.utime(old, (time.time() - 100, time.time() - 100))

    result = _archive_models(company)

    assert new.exists() and new.read_text() == "new"
    assert not old.exists()
    assert (agent / "yaml1history" / old.name).exists()
    assert result["archived_yaml1"] == [old.name]


def test_archive_models_keeps_latest_moves_rest_deletes_locks(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    agent = company / "Agent"
    agent.mkdir()
    old_model = company / "测试公司Model-260624.xlsx"
    new_model = company / "测试公司Model-260626.xlsx"
    lock = company / "~$测试公司Model-260624.xlsx"
    old_model.write_bytes(b"old")
    new_model.write_bytes(b"new")
    lock.write_bytes(b"lock")

    result = _archive_models(company)

    assert new_model.exists()
    assert not old_model.exists()
    assert (agent / "Modelhistory" / old_model.name).exists()
    assert not lock.exists()
    assert result["archived_models"] == [old_model.name]
    assert result["deleted_locks"] == [lock.name]


def test_archive_guard_latest_yaml1_untouched(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    agent = company / "Agent"
    agent.mkdir()
    only = agent / "yaml1_测试公司_20260626.yaml"
    only.write_text("only")
    result = _archive_models(company)
    assert only.exists()
    assert result["archived_yaml1"] == []


def test_archive_guard_no_models_no_locks(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    result = _archive_models(company)
    assert result == {"archived_yaml1": [], "archived_models": [], "deleted_locks": []}


def test_archive_guard_forecast_not_touched(tmp_path: Path) -> None:
    company = _make_company(tmp_path)
    agent = company / "Agent"
    agent.mkdir()
    fc = agent / "forecast"
    fc.mkdir()
    (fc / "dcf_summary.json").write_text("{}")
    _archive_models(company)
    assert (fc / "dcf_summary.json").exists()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `py -m pytest tests/test_workbench_home.py -v`
Expected: FAIL `ImportError: cannot import name '_archive_models'`

- [ ] **Step 3: 写最小实现**

在 `src/workbench.py` 顶部 import 区加（若未有）：`import shutil`、`import subprocess`（subprocess 已在 line 18 import）。在 `_folder_overview_signals` 后插入：

```python
def _git_mv(src: Path, dst: Path) -> None:
    """git mv 保历史；非 tracked 回退 shutil.move。dst 的父目录须已存在。"""
    try:
        subprocess.run(
            ["git", "mv", str(src), str(dst)],
            cwd=BASE_DIR, check=True, capture_output=True,
        )
    except (subprocess.CalledProcessError, FileNotFoundError):
        shutil.move(str(src), str(dst))


def _unique_dst(dst_dir: Path, name: str) -> Path:
    dst = dst_dir / name
    if not dst.exists():
        return dst
    stamp = time.strftime("%H%M%S")
    return dst_dir / f"{name.rsplit('.', 1)[0]}-{stamp}.{name.rsplit('.', 1)[1]}"


def _model_vintage_key(path: Path) -> str:
    m = _MODEL_DATE_RE.search(path.stem)
    return m.group(1) if m else "000000"


def _archive_models(company_dir: Path) -> dict[str, Any]:
    agent = agent_dir(company_dir)
    yaml1history = agent / "yaml1history"
    modelhistory = agent / "Modelhistory"

    archived_yaml1: list[str] = []
    latest_yaml1 = _latest_yaml1(company_dir)
    if latest_yaml1 is not None:
        yaml1s = sorted(agent.glob("yaml1*.yaml"))
        if len(yaml1s) > 1:
            yaml1history.mkdir(parents=True, exist_ok=True)
            for p in yaml1s:
                if p.resolve() == latest_yaml1.resolve():
                    continue
                dst = _unique_dst(yaml1history, p.name)
                _git_mv(p, dst)
                archived_yaml1.append(p.name)

    archived_models: list[str] = []
    excels = [p for p in company_dir.glob("*.xlsx") if not p.name.startswith("~$")]
    if len(excels) > 1:
        modelhistory.mkdir(parents=True, exist_ok=True)
        newest = max(excels, key=_model_vintage_key)
        for p in excels:
            if p.resolve() == newest.resolve():
                continue
            dst = _unique_dst(modelhistory, p.name)
            _git_mv(p, dst)
            archived_models.append(p.name)

    deleted_locks: list[str] = []
    for lock in company_dir.glob("~$*.xlsx"):
        lock.unlink()
        deleted_locks.append(lock.name)

    return {
        "archived_yaml1": archived_yaml1,
        "archived_models": archived_models,
        "deleted_locks": deleted_locks,
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `py -m pytest tests/test_workbench_home.py -v`
Expected: 13 passed

- [ ] **Step 5: 提交**

```bash
git add tests/test_workbench_home.py src/workbench.py
git commit -m "feat(workbench): add _archive_models helper (yaml1history/Modelhistory)"
```

---

## Task 4: 后端 — `POST /api/companies/{id}/archive-models` 端点

**Files:**
- Modify: `src/workbench.py`（插在 `regenerate_forecast` 端点后，约 line 2437 区）

- [ ] **Step 1: 加端点**

在 `regenerate_forecast` 端点后插入：

```python
@app.post("/api/companies/{company_id}/archive-models")
def archive_models(company_id: str) -> dict[str, Any]:
    company_dir = _company_dir(company_id)
    return _archive_models(company_dir)
```

- [ ] **Step 2: 跑全部 workbench home 测试 + py_compile**

Run: `py -m pytest tests/test_workbench_home.py -v && py -m py_compile src/workbench.py`
Expected: 13 passed + 编译通过

- [ ] **Step 3: 手动冒烟（用真实公司）**

Run: `py -c "from src.workbench import _folder_overview_signals, _company_dirs; [print(c.name, _folder_overview_signals(c)['pipeline_stage']) for c in _company_dirs()]"`
Expected: 4 家公司各打印一个 stage（青岛啤酒=初始化完毕，会稽山/赛维=建模完毕，新乳业=建模完毕且有DA表）

- [ ] **Step 4: 提交**

```bash
git add src/workbench.py
git commit -m "feat(workbench): add POST /api/companies/{id}/archive-models endpoint"
```

---

## Task 5: 前端 — 类型 + Sidebar 首页条目 + App 路由

**Files:**
- Modify: `app/src/types.ts`
- Modify: `app/src/App.tsx`（`Sidebar` 组件 line 215-249；`App` 组件 line 4339-4447）

- [ ] **Step 1: 加类型**

在 `app/src/types.ts` 末尾追加：

```typescript
export type PipelineStage =
  | "未初始化"
  | "初始化完毕"
  | "核心假设完毕"
  | "建模完毕"
  | "建模完毕且有DA表";

export type HomeFolderOverviewSignals = {
  pipeline_stage: PipelineStage;
  yaml1_date: string | null;
  yaml1_versions: number;
  yaml1_archive_eligible: boolean;
  root_models: { excel_count: number; lock_count: number; archive_eligible: boolean };
  workbench_materials: { reports: number; notes: number; collected: number; important: number };
  agent_materials: { load: number; brkd: number; top_weight: number; adj: number; pjbg: number };
};

export type HomeFolderOverview = {
  company_id: string;
  name: string;
  code: string;
  signals: HomeFolderOverviewSignals | null;
  error: string | null;
};

export type ArchiveModelsResult = {
  archived_yaml1: string[];
  archived_models: string[];
  deleted_locks: string[];
};

export type HomeTab = "folder-overview";
```

- [ ] **Step 2: 改 Sidebar 加首页条目**

把 `Sidebar` 组件签名加 `homeActive` 和 `onSelectHome` props，在 `brand` 区下、`Companies` 列表上加首页按钮。修改 `app/src/App.tsx` line 215-249 的 `Sidebar`：

```tsx
function Sidebar({
  companies,
  selectedId,
  onSelect,
  loading,
  homeActive,
  onSelectHome,
}: {
  companies: CompanySummary[];
  selectedId?: string;
  onSelect: (id: string) => void;
  loading: boolean;
  homeActive: boolean;
  onSelectHome: () => void;
}) {
  return (
    <aside className="sidebar">
      <div className="brand">
        <div className="brand-title">ModelKing</div>
        <div className="brand-subtitle">Buy-side workbench</div>
      </div>
      <button
        className={`company-item home-item ${homeActive ? "selected" : ""}`}
        onClick={onSelectHome}
        type="button"
      >
        <span className="company-name">首页</span>
        <span className="company-code">Home</span>
      </button>
      <div className="sidebar-section-label">Companies</div>
      <div className="company-list">
        {loading ? <div className="activity">Loading companies</div> : null}
        {companies.map((company) => (
          <button
            className={`company-item ${!homeActive && selectedId === company.id ? "selected" : ""}`}
            key={company.id}
            onClick={() => onSelect(company.id)}
            type="button"
          >
            <span className="company-name">{company.name}</span>
            <span className="company-code">{company.code}</span>
          </button>
        ))}
      </div>
    </aside>
  );
}
```

- [ ] **Step 3: 改 App 加 homeActive 状态 + 主区条件渲染**

修改 `App` 组件（line 4339-4447）。在 state 区加：

```tsx
const [homeActive, setHomeActive] = useState(true);  // 默认进首页
```

改 `loadCompanies` 里 `setSelectedId((current) => current ?? result[0]?.id)` → 改为不自动选中公司（保留首页）：删掉这行 `setSelectedId`，或改为 `setSelectedId(undefined)`。

把 `Sidebar` 调用改为：

```tsx
<Sidebar
  companies={companies}
  loading={loading}
  onSelect={(id) => { setSelectedId(id); setHomeActive(false); }}
  selectedId={selectedId}
  homeActive={homeActive}
  onSelectHome={() => { setHomeActive(true); setSelectedId(undefined); }}
/>
```

主区 `<main className="main-pane">` 内部改为条件渲染：当 `homeActive` 为 true 时渲染 `<FolderOverview />`，否则渲染现有 topbar/tabbar/DetailView。最简改法：在 `<main>` 开头加：

```tsx
{homeActive ? (
  <FolderOverview />
) : (
  <>
    {/* 现有 topbar + tabbar + error + detail 渲染全部包进这个 fragment */}
  </>
)}
```

把现有 `<header className="topbar">…` 到 `{!detailLoading && !detail ? <EmptyState …/> : null}` 整段移进 `<>…</>`。

- [ ] **Step 4: 暂时占位 FolderOverview + 编译验证**

在 `App.tsx` `App` 组件前加最小占位组件（Task 6 再充实）：

```tsx
function FolderOverview() {
  return <div className="view-stack"><h1>文件夹总览</h1><p>（待实现）</p></div>;
}
```

并在文件顶部 import 加 `HomeFolderOverview`、`ArchiveModelsResult` 类型（Task 6 用）。

Run: `cd app && npm run build`
Expected: 构建通过，无 TS 报错

- [ ] **Step 5: 提交**

```bash
git add app/src/types.ts app/src/App.tsx
git commit -m "feat(app): add 首页 entry in sidebar + homeActive routing"
```

---

## Task 6: 前端 — `FolderOverview` 组件（信号表 + 归档按钮）

**Files:**
- Modify: `app/src/App.tsx`（替换 Task 5 的占位 `FolderOverview`）

- [ ] **Step 1: 实现组件**

用真实 `FolderOverview` 替换占位（行=信号、列=公司）：

```tsx
const HOME_TABS: Array<{ key: HomeTab; label: string }> = [
  { key: "folder-overview", label: "文件夹总览" },
];

function FolderOverview() {
  const [rows, setRows] = useState<HomeFolderOverview[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string>();
  const [archiving, setArchiving] = useState<string | null>(null);
  const [homeTab, setHomeTab] = useState<HomeTab>("folder-overview");

  async function load() {
    setLoading(true);
    setError(undefined);
    try {
      setRows(await apiGet<HomeFolderOverview[]>("/api/home/folder-overview"));
    } catch (err) {
      setError(String(err));
    } finally {
      setLoading(false);
    }
  }

  useEffect(() => { void load(); }, []);

  async function archive(companyId: string, kind: "yaml1" | "models") {
    if (!window.confirm(kind === "yaml1" ? "归档历史 yaml1？只留最新一份。" : "归档根目录历史模型文件并删除锁文件？")) return;
    setArchiving(companyId);
    try {
      await apiPost<ArchiveModelsResult>(`/api/companies/${encodeURIComponent(companyId)}/archive-models`);
      await load();
    } catch (err) {
      setError(String(err));
    } finally {
      setArchiving(null);
    }
  }

  const stageTone: Record<string, string> = {
    "未初始化": "stage-0",
    "初始化完毕": "stage-1",
    "核心假设完毕": "stage-2",
    "建模完毕": "stage-3",
    "建模完毕且有DA表": "stage-4",
  };

  return (
    <div className="view-stack folder-overview-page">
      <header className="topbar">
        <div>
          <div className="eyebrow">Home</div>
          <h1>文件夹总览</h1>
        </div>
      </header>
      <nav className="tabbar">
        {HOME_TABS.map((t) => (
          <button key={t.key} className={homeTab === t.key ? "active" : ""} onClick={() => setHomeTab(t.key)} type="button">{t.label}</button>
        ))}
      </nav>
      {error ? <div className="error-banner">{error}</div> : null}
      {loading ? <div className="activity content-activity">Loading folder overview</div> : null}
      {!loading && homeTab === "folder-overview" ? (
        <div className="table-scroll workbook-scroll">
          <table className="financial-table folder-overview-table">
            <thead>
              <tr>
                <th>信号</th>
                {rows.map((r) => (
                  <th key={r.company_id} className="company-col">
                    <span className="company-name">{r.name}</span>
                    <span className="company-code">{r.code}</span>
                  </th>
                ))}
              </tr>
            </thead>
            <tbody>
              <tr>
                <td className="signal-label">建模管线推进</td>
                {rows.map((r) => (
                  <td key={r.company_id} className={`stage-cell ${stageTone[r.signals?.pipeline_stage ?? ""] ?? ""}`}>
                    {r.signals?.pipeline_stage ?? "读取失败"}
                  </td>
                ))}
              </tr>
              <tr>
                <td className="signal-label">yaml1 日期</td>
                {rows.map((r) => (
                  <td key={r.company_id} className="numeric">{r.signals?.yaml1_date ?? "-"}</td>
                ))}
              </tr>
              <tr>
                <td className="signal-label">yaml1 历史版本</td>
                {rows.map((r) => (
                  <td key={r.company_id} className="numeric">
                    {r.signals?.yaml1_versions ?? "-"}
                    {r.signals?.yaml1_archive_eligible ? (
                      <button className="archive-btn" disabled={archiving === r.company_id} onClick={() => archive(r.company_id, "yaml1")} type="button">归档</button>
                    ) : null}
                  </td>
                ))}
              </tr>
              <tr>
                <td className="signal-label">根目录模型文件</td>
                {rows.map((r) => (
                  <td key={r.company_id} className="numeric">
                    {r.signals ? `${r.signals.root_models.excel_count} (${r.signals.root_models.lock_count} 锁)` : "-"}
                    {r.signals?.root_models.archive_eligible ? (
                      <button className="archive-btn" disabled={archiving === r.company_id} onClick={() => archive(r.company_id, "models")} type="button">归档</button>
                    ) : null}
                  </td>
                ))}
              </tr>
              <tr>
                <td className="signal-label">工作台素材数</td>
                {rows.map((r) => (
                  <td key={r.company_id} className="numeric">
                    {r.signals ? `研${r.signals.workbench_materials.reports} 纪${r.signals.workbench_materials.notes} 收${r.signals.workbench_materials.collected} 重${r.signals.workbench_materials.important}` : "-"}
                  </td>
                ))}
              </tr>
              <tr>
                <td className="signal-label">Agent技能素材数</td>
                {rows.map((r) => (
                  <td key={r.company_id} className="numeric">
                    {r.signals ? `L${r.signals.agent_materials.load} B${r.signals.agent_materials.brkd} 权${r.signals.agent_materials.top_weight} A${r.signals.agent_materials.adj} P${r.signals.agent_materials.pjbg}` : "-"}
                  </td>
                ))}
              </tr>
            </tbody>
          </table>
        </div>
      ) : null}
    </div>
  );
}
```

- [ ] **Step 2: 编译验证**

Run: `cd app && npm run build`
Expected: 构建通过

- [ ] **Step 3: 手动验证**

Run: `py -m src.workbench`（先确认 8765 端口空闲），浏览器开 `http://127.0.0.1:8765`：
- 默认进首页，看到文件夹总览表，4 家公司 6 行信号。
- 青岛啤酒列 stage=初始化完毕、yaml1 日期=-。
- 新乳业列 yaml1 历史版本=3、根目录模型文件显示归档按钮；点归档 → 确认 → 表刷新，版本变 1。
- 点 Sidebar 任一公司 → 进公司视图（现有 tabs 正常）。点首页 → 回总览。

验证后杀掉 workbench 进程。

- [ ] **Step 4: 提交**

```bash
git add app/src/App.tsx
git commit -m "feat(app): implement FolderOverview table with archive buttons"
```

---

## Task 7: 前端 — 样式

**Files:**
- Modify: `app/src/styles.css`

- [ ] **Step 1: 加样式**

在 `app/src/styles.css` 末尾追加（色值用 CLAUDE.md 规定的 CSS 变量 `--blue:#003d7a`、`--red:#b42318`，先确认文件顶部已声明，若已有变量则复用）：

```css
.home-item { margin-bottom: 0.5rem; }
.home-item .company-code { color: var(--blue, #003d7a); }

.folder-overview-table th.company-col,
.folder-overview-table td { text-align: center; vertical-align: middle; }
.folder-overview-table .signal-label { text-align: left; font-weight: 600; background: var(--row-alt, #f7f8fa); }
.folder-overview-table .company-col .company-name { display: block; }
.folder-overview-table .company-col .company-code { display: block; font-size: 0.75rem; color: #888; }

.stage-cell { font-weight: 600; }
.stage-0 { color: var(--red, #b42318); }
.stage-1 { color: #8a6d3b; }
.stage-2 { color: #6c757d; }
.stage-3 { color: var(--blue, #003d7a); }
.stage-4 { color: #2f7d32; }

.archive-btn {
  margin-left: 0.5rem; padding: 0.1rem 0.5rem; font-size: 0.75rem;
  border: 1px solid var(--blue, #003d7a); color: var(--blue, #003d7a);
  background: transparent; border-radius: 4px; cursor: pointer;
}
.archive-btn:disabled { opacity: 0.5; cursor: default; }
```

- [ ] **Step 2: 编译 + 视觉验证**

Run: `cd app && npm run build`，再 `py -m src.workbench` 打开看视觉效果（首页条目、stage 颜色、归档按钮）。验证后杀进程。

- [ ] **Step 3: 提交**

```bash
git add app/src/styles.css
git commit -m "style(app): folder overview table + home sidebar entry"
```

---

## Task 8: 文档同步 + 全量验证

**Files:**
- Modify: `docs/ARCHITECTURE.md`、`docs/CHANGELOG.md`、`docs/数据流水线.md`

- [ ] **Step 1: 更新 `docs/数据流水线.md`**

在 workbench 段落补一行：workbench 新增首页「文件夹总览」只读视图（`GET /api/home/folder-overview`）+ 一键归档历史模型动作（`POST /api/companies/{id}/archive-models`，yaml1 → `Agent/yaml1history/`、根目录 Excel → `Agent/Modelhistory/`、`~$` 锁文件删除）。

- [ ] **Step 2: 更新 `docs/ARCHITECTURE.md`**

在 workbench 章节加：首页模式（`homeActive`）作为 Sidebar 特殊条目，文件夹总览 subtab 展示全公司健康度信号表。

- [ ] **Step 3: 更新 `docs/CHANGELOG.md`**

表首追加一行（日期 2026-06-26）：`feat(workbench): 首页文件夹总览 + 一键归档历史模型（yaml1history/Modelhistory）`。

- [ ] **Step 4: 全量测试**

Run: `py -m pytest tests/test_workbench_home.py tests/test_workbench_assumption_editor.py tests/test_workbench_da_view.py -v`
Expected: 全 passed

- [ ] **Step 5: 提交**

```bash
git add docs/ARCHITECTURE.md docs/CHANGELOG.md docs/数据流水线.md
git commit -m "docs: sync 首页文件夹总览 + 归档动作"
```

---

## 风险与守卫

- **latest 判定**：yaml1 用 `latest_yaml1_path()`（按 mtime），测试用 `os.utime` 控制；Excel 用文件名 6 位日期串。无日期串的 Excel 视为最旧（`000000`），保守归档。
- **git mv 回退**：非 tracked 文件 `git mv` 会失败，回退 `shutil.move`，不报错。
- **绝不删 latest / `~$` 以外文件**：归档循环显式 `if p.resolve() == latest.resolve(): continue`；锁文件只删 `~$*.xlsx` glob；`forecast/`、`recon/`、`.modelking/` 不在 glob 范围。
- **单公司异常不拖垮整表**：`_folder_overview_signals` 包 try/except，失败列显示「读取失败」。
- **默认进首页**：`homeActive` 初值 true，符合"首页是入口"语义。
