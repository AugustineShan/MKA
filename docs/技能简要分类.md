# 技能简要分类

> 给 Agent 的快速分流卡片。这里帮助你先判断该用哪个 skill；真正执行前仍要读取对应 `.claude/skills/*/SKILL.md` 和它加载的最新版动态 runbook。
>
> **首次冷启动**先读 `docs/快速上手.md`（端到端主线），本文件用于任务级的 skill 分流。

## 一句话总览

```text
/init -> clean 历史事实
Alphapai业务拆分抓取器 -> 业务拆分历史 factpack
/brkd 或 /load 或 Alphapai-load -> 候选理解
/ka -> 正式核心假设
/comp -> yaml1 + forecast
/adj / /frontend-edit / /annual-update -> 已有正式稿的安全修改
/da -> 重资产 DA/CAPEX 专项排程
/pjbg -> 评级报告研究结论（标题+短期+中长期+投资建议）+ 注入 docx 模板出评级报告
```

核心原则：

- 核心假设.md 是判断源头；`yaml1`、`forecast_params.yaml`、`Agent/forecast/` 都是派生产物。
- **主导方向**：`/brkd`、`/load` 先忠实抓业务拆分+详细历史+业务数据（即便不参与计算、只进收纳区），预测旋钮是次要 draft 候选；`/ka` 拿 brkd/load/alphapai 产物和分析师裁决预测，同时忠实收集与预测有关的历史防丢数。预测是末，业务结构与历史是本。
- Alphapai业务拆分抓取器只交业务拆分历史 factpack，不写预测。
- `/brkd`、`/load`、Alphapai-load 只交候选理解，不拍正式判断。
- `/ka` 负责全量裁决；已有正式稿的局部变化不要回到 `/ka` 做 modify。
- `/comp` 只翻译，不研究、不重判。
- BS/CF/DCF 驱动默认不在理解层主动预测；重资产 DA/CAPEX 走 `/da`。
- brkd/load/alphapai 参考稿统一落 `Skills素材包/KA（ALPHAPAI拆出来的东西放在这里）/`，命名 `核心假设参考{来源}_{YYYYMMDD}.md`；`/ka` 到该目录找参考稿，根目录只剩 official `核心假设.md`。

## 快速分流

| 你现在要做什么 | 用哪个入口 | 结果 | 不该做什么 |
|---|---|---|---|
| 拉取或刷新公司财务数据，生成可信历史宽表 | `/init` | `Agent/data.db`、clean 表、核心指标速览、年报/季报材料 | 不改 `raw_tushare`，不写判断 |
| 读研报/纪要/年报，先整理业务和利润表讨论 | `/brkd` | `Agent业务讨论.md` | 不生成正式核心假设，不写 yaml1 |
| 装载一个旧 Excel 模型，保存其当时时间轴 | `/load` | KA 参考稿区 `核心假设参考load_{YYYYMMDD}.md`、`Agent/Load/{load_id}/` 沙箱 | 不用后验事实修模型，不覆盖正式 forecast |
| 把 `/load` 包给网页端长上下文模型处理 | `/webload` | `WEBCLAUDE/模型装载部分/` 执行包 | 不理解模型、不生成假设、不编译 |
| 把 `/ka` 包给网页端长上下文模型处理 | `/webka` | `WEBCLAUDE/webka(Claude帮你统摄核心假设）/` 3 份合并 md | 纯打包，回收手动走本地 `/ka`；不本地裁决、不落盘 |
| 让 Alphapai 专门抓非标准业务拆分历史 | `docs/Alphapai/Alphapai业务拆分抓取器.md` | `Alphapai业务拆分事实底稿.md` | 只抓历史 factpack；不写预测、不写 knobs、不写 terminal |
| 让 Alphapai 网页端挖它自己的金融数据库 | `docs/Alphapai/Alphapai-load核心假设参考提示词.md` | KA 参考稿区 `核心假设参考alphapai_{YYYYMMDD}.md` | 不是本地 skill；只产 reference，不开 BS/CF 闸 |
| 把最高权重材料、BRKD、LOAD 和 clean 事实裁成正式稿 | `/ka` | `{公司}-{YYYYMMDD}-核心假设.md` | 不读原始 Excel/PDF，不做旧稿 modify |
| 把正式核心假设翻译成机器覆盖层并跑 DCF | `/comp` | `Agent/yaml1_*.yaml`、`forecast_params.yaml`、`Agent/forecast/` | 不改判断、不替原文算数 |
| 已有正式稿，只改已有 knobs 的少量数值 | `/adj quick` | 新核心假设 + scoped yaml1 patch + forecast | 不新增 path、结构、horizon、fade |
| 已有正式稿，读新增材料后系统性小改 | `/adj incremental` | 新核心假设，再走 `/comp` | 推翻骨架时不要硬补丁，转 `/ka` 重建 |
| 前端沙盘试算已拍板，要正式落盘 | `/frontend-edit` | 定点 patch 核心假设、knobs、yaml1，再 forecast | 不读新材料，不重跑 compiler 全文 |
| 新年报已出，旧预测一部分变成真实历史 | `/annual-update` | 新日期核心假设 + 新 yaml1/forecast | 不在脏 clean 上滚，不覆盖旧稿 |
| 重资产公司需要固定资产、在建工程、折旧、capex 排程 | `/da` | `Agent/recon/da_facts_latest.json`、`Agent/da_schedule.yaml` | 轻资产别用；不要在 `/ka` 自己造 cohort |
| 生成评级报告「研究结论」+ 注入 docx | `/pjbg` | `内部报告/评级报告/{公司}-{日期}-研究结论.md` + `…评级报告.docx` | 方向只从判断文件、数字只从 forecast；R08 盈利预测全表从 forecast 渲染；不碰表头/投资要点/正文 |

## 分层地图

### 1. 数据地基层

**`/init`**（完整文档见 `docs/init技能文档.md`）

- 读：公司名/代码、TuShare、公告、已有 approved override。
- 写：`raw_tushare` 镜像、`clean_annual`、`clean_quarterly`、`core_metrics_overview.*`、`OfficialBreakdowns`（官方营收拆分）、公告 Markdown、财务费用档案、`defaults.yaml`。
- 停：clean hard check 未闭合（exit 3）、raw 被要求手改、关键历史事实缺口。
- 交给：`/brkd`、`/ka`、`/comp`、`/annual-update`、工作台。

### 2. 候选理解层

**`/brkd`**

- 用途：把研报/纪要/年报和 clean 历史整理成业务与利润表预讨论。
- 主导方向：忠实记录业务拆分+历史+收纳数据（即便不参与计算）优先于预测。
- 产物：`Agent业务讨论.md`（公司根目录），状态是 draft，所有预测建议待 `/ka` 拍板；可选参考稿落 KA 参考稿区 `核心假设参考brkd_{YYYYMMDD}.md`。
- 边界：不锁最终时间轴，不写正式 `核心假设.md`，不写 yaml1，不跑 DCF。

**`/load`**

- 用途：把唯一 Excel 模型按它自己的 vintage 时间轴装载出来。
- 主导方向：保真装载业务拆分+历史+收纳数据（即便不参与计算）优先于搬运预测。
- 产物：KA 参考稿区 `核心假设参考load_{YYYYMMDD}.md` + `Agent/Load/{load_id}/` 沙箱副本、`yaml1_load_*` 和沙箱 forecast。
- 边界：`model_boundary.*` 是时间轴归口；禁读后验材料；不覆盖正式 `Agent/forecast/`。

**`/webload`**

- 用途：为网页端执行 `/load` 打包 allowed materials、禁读清单、边界和执行说明。
- 产物：`WEBCLAUDE/模型装载部分/`。
- 边界：只 prepare + 打包，不理解模型、不写核心假设、不编译。

**`/webka`**

- 用途：为网页端执行 `/ka` 打包规则与材料（4 份规则 + 最高权重材料 + BRKD/LOAD/reference/旧稿 + `defaults.yaml` + `core_metrics_overview` / `OfficialBreakdowns`）。
- 产物：`WEBCLAUDE/webka(Claude帮你统摄核心假设）/` 下 3 份合并 md（`readme first.md` / `必读和素材.md` / `不必要读强制碰到再速查.md`）。
- 边界：纯打包，本地先跑 `ka_prepare` + 强制 §2/§6b 门禁；不在本地裁决、不落盘核心假设；网页端产出的稿子由用户拷回本地走 `/ka` 落盘（`ka_archive` + 写今日新稿 + `/comp`）。

**Alphapai-load prompt**

- 用途：让 Alphapai 用它自己的金融数据库吐出 KA 参考稿区 `核心假设参考alphapai_{YYYYMMDD}.md`。
- 上游：可先用 `docs/Alphapai/Alphapai业务拆分抓取器.md` 产出 `Alphapai业务拆分事实底稿.md`，专门抓用户指定主拆分、桥表和高价值辅助拆分历史。
- 产物：reference 稿，落 KA 参考稿区，只供后续裁决。
- 边界：不是本地启动器；若有业务拆分 factpack，必须优先承接，不得把用户指定主拆分替换成官方更稳健口径；BS/CF/DCF 线索一律收纳，不能自行判断核心 thesis，不能写进 `knobs`。

**Alphapai业务拆分抓取器**

- 用途：让 Alphapai 专门抓 `/init` 标准财务表里没有的非标准业务拆分历史数值；支持你直接给 `部` / `leaf` 表后按表定向取数。
- 产物：`{公司}-{日期}-Alphapai业务拆分事实底稿.md`，状态 `factpack/reference`。
- 边界：只抓历史，不写预测、不写 `knobs`、不写 `horizon`/`terminal`；用户指定主拆分或定向 leaf 表必须锁定为主轴，官方口径只能作为桥表或 sanity check；最近 5 年完整性优先，早期数据有参考价值才抓。

### 3. 正式裁决层

**`/ka`**

- 用途：把最高权重材料、BRKD、LOAD、`/init` 事实和分析师 thesis 收口成正式 `核心假设.md`；到 KA 参考稿区找 brkd/load/alphapai 参考稿。
- 主导方向：拿 brkd/load/alphapai 产物和分析师裁决预测，同时忠实收集与预测有关的历史防丢数。
- 必须先做：时间轴四数、接缝总账、骨架门、数值门。
- 产物：公司根目录 `{公司}-{YYYYMMDD}-核心假设.md`，状态 official，末尾有 official `knobs`。
- 边界：不做旧稿局部 modify；不产 yaml1；默认只裁利润表 + 业务层盈利模型。

### 4. 编译运行层

**`/comp`**

- 用途：把 official `核心假设.md` 忠实翻译成 `yaml1`，通过 fidelity/audit 后跑 forecast。
- 读：official 核心假设、`defaults.yaml`、`数据格式参考.md`、`yaml1算法模板契约.md`、`knobs块契约.md`、`yaml1前端展示契约.md`。
- 写：`Agent/yaml1_*.yaml`、`Agent/.modelking/forecast_params.yaml`、`Agent/forecast/`。
- 边界：不读 PDF，不改原文判断，不用 defaults 反向否定核心假设。

### 5. 正式稿调整层

**`/adj quick`**

- 用途：已有 official 核心假设下，只拨已有 `knobs` 数值。
- 写：归档旧稿、新核心假设、scoped yaml1 patch、新 forecast。
- 边界：白名单不可加宽；不得改结构、slug、family、horizon、fade、历史、来源、stash。

**`/adj incremental`**

- 用途：读 ADJ 增量材料，讨论系统性影响后修改核心假设源文，再走 `/comp`。
- 边界：新增结构或推翻 thesis 基础时，不硬补丁，转 `/ka` 重建。

**`/frontend-edit`**

- 用途：前端 assumption-preview 已试算并拍板后，把变更定点回写到语义源头。
- 写：核心假设正文、`knobs`、对应 yaml1 值、forecast。
- 边界：不读新材料；映射不到 path 立即停；`terminal.explicit_end` 和 `fade.to_year` 属结构改动。

**`/annual-update`**

- 用途：新年报出来后，把旧核心假设滚到最新实际年。
- 流程：读旧稿总账 -> `/init` 刷数据 -> fetcher 填标准线 -> 滚后时间轴确认 -> 估算/重定未来 -> `/comp`。
- 边界：旧稿只读另存；clean 失败停；拿不到的实际只能估算带旗或待补。

### 6. 专项外挂层

**`/da`**

- 用途：重资产公司专用 DA/CAPEX 排程。
- 事实层：`Agent/recon/da_facts_latest.json`。
- 假设层：`Agent/da_schedule.yaml`。
- 边界：LLM 抽事实和参与商议，Python `da_roll` 计算；`da_schedule.base_year` 必须等于 defaults base period。

### 7. 报告产出层

**`/pjbg`**

- 用途：把 `/ka` 核心假设 + `/comp` forecast + PJBG 素材区事实，合成评级报告「研究结论」四件（标题+短期+中长期+投资建议），并注入 docx 模板产出正式评级报告。
- 读：`公司判断和最新观点.md` + 最新 `*核心假设.md`（A/B 层判断）；`Skills素材包/PJBG评级报告素材区/`（C 层事实）；`Agent/forecast/derived_metrics_annual.csv`（D 层数字）；空白模板 `.claude/skills/pjbg/template/评级报告模板.docx`（可 `PJBG_TEMPLATE_PATH` 覆盖）。
- 写：`内部报告/评级报告/{公司}-{YYYYMMDD}-研究结论.md` + `…评级报告.docx`（R01 标题 + R06 研究结论 + R08 盈利预测全表，按节标题定位注入，字体随模板、加粗随 .md）。
- 边界：方向/落点只从 A/B、数字只从 D、C 只供事实；R08 盈利预测全表从 forecast CSV 渲染（对齐评级报告模板 R08 体例：ROE 在 EPS 前、(百万) 标注、不带 +/x；年份窗同前端）；不碰表头字段/投资要点/正文/声明；判断缺失/素材空/forecast 缺 → 停下问分析师，不自填。模板可替换（按节标题定位，换模板通常不改代码）。

## 共享真源和动态 runbook

| 类型 | 文件 | 谁用 | 作用 |
|---|---|---|---|
| 横切纪律 | `skills/核心纪律_skill_v1.md` | `/brkd`、`/load`、`/ka`、`/adj`、`/annual-update` | A1-A7：历史保全、接缝、分段确认、knobs 同源等 |
| 源语言语法 | `skills/核心假设源语言_skill_v1.md` | 写或改核心假设源文的技能 | 定义 `核心假设.md` 应该长什么样 |
| knobs 契约 | `docs/knobs块契约.md` | 所有涉及 `knobs` 的技能 | 末尾 `knobs` 块单一真源 |
| yaml1 前端展示契约 | `docs/yaml1前端展示契约.md` | `/comp`、工作台 | `display` 块单一真源，决定 stash 展示去向 |
| KA 编辑器 | `skills/核心假设编辑器_skill_v1.md` | `/ka` | 全量裁决 runbook |
| BRKD 预理解器 | `skills/业务预理解器_skill_v3.md` | `/brkd` | 业务讨论草稿 runbook |
| 模型装载器 | `skills/模型装载器_skill_v3.md` | `/load`、`/webload` | Excel vintage 装载 runbook |
| Alphapai业务拆分抓取器 | `docs/Alphapai/Alphapai业务拆分抓取器.md` | Alphapai-load、`/ka` | 网页端 factpack prompt，只抓业务拆分历史 |
| 年度更新器 | `skills/年度更新器_skill_v1.md` | `/annual-update` | 年报滚动 runbook |
| 假设调整器 | `skills/核心假设调整器_skill_v1.md` | `/adj` | quick/incremental runbook |
| DA 排程器 | `skills/da_折旧摊销排程_skill_v1.md` | `/da` | 重资产事实抽取与排程商议 |
| YAML1 compiler | `skills/yaml1compiler_v5.md` | `/comp` | `核心假设.md` -> `yaml1` 翻译规则 |

## 常见误分流

| 误用 | 正确去处 |
|---|---|
| 想让 `/ka` 直接读原始 Excel | 先 `/load` |
| 想让 `/ka` 读一堆研报纪要 | 先 `/brkd` |
| 想让 Alphapai-load 顺手抓高/中/低端多年拆分 | 先跑 Alphapai业务拆分抓取器，再把 factpack 贴给 Alphapai-load |
| 有正式稿后让 `/ka` 改几个数 | `/adj quick` 或 `/frontend-edit` |
| 新年报已覆盖旧预测起点还继续 `/comp` | `/annual-update` |
| 在 `/brkd` 或 Alphapai-load 里写 capex/折旧逐年预测 | 收纳；需要时走 `/da` |
| 手改 `forecast_params.yaml` 或 `Agent/forecast/` | 回到 `核心假设.md` 或数据源 |
| 把 `yaml1` 当判断源头 | `核心假设.md` 才是判断源头 |

## Agent 执行提示

1. 先判断当前任务属于哪一层。
2. 读取对应 `.claude/skills/{skill}/SKILL.md`。
3. 若该启动器要求动态加载最新版 `skills/*_skill_vN.md`，按版本号最大者加载。
4. 写正式产物前检查门禁：时间轴、source-of-truth、clean 状态、knobs 同源、BS/CF 边界。
5. 汇报时说清楚：读了什么、写了什么、停在哪些门禁前或已经通过哪些门禁。
